/**
 * BarberStack — Theme Barrel Export
 *
 * Importe TUDO de um único lugar:
 * import { Colors, Spacing, Typography, Shadows } from '@/shared/theme';
 */

export { Colors } from "./colors";
export { Shadows } from "./shadows";
export { BorderRadius, Layout, Spacing } from "./spacing";
export {
    FontSize,
    FontWeight, LetterSpacing, LineHeight, Typography
} from "./typography";

